from datetime import datetime


nome = input("Digite seu nome")
print (f"Ola, {nome}, seja bem vindo!")

data_nascimento = input("Digite sua data de nascimento (dd/mm/aaaa): ")
data_convertida = datetime.strptime(data_nascimento, "%d/%m/%Y")
print("Voce nasceu em:", data_convertida.strftime("%D de %B de %Y"))

from datetime import datetime 

data_nascimento = input("Digite sua data de nascimento (dd/mm/aaaa): ")
data_convertida = datetime.strptime(data_nascimento, "%d/%m/%Y")

hoje = datetime.today()

idade = hoje.year - data_convertida.year

# Verificação se ja fez aniversario esse ano

if (hoje.month, hoje.day) < (data_convertida.month, data_convertida.day):
    idade -=1

    print("Sua idade é:", idade)

cpf = input("Digite seu CPF: ")
print (f"Seu CPF é {cpf}")
